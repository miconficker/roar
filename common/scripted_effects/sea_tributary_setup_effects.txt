
ROA_setup_confederation_effect = {
	title:c_abzakhia.holder = {

		title:c_temir.holder = { add_to_list = circassia_confederates }
		title:c_uerp.holder = { add_to_list = circassia_confederates }
		title:c_labeshkhua.holder = { add_to_list = circassia_confederates }
		title:c_abazinia.holder = { add_to_list = circassia_confederates }
		title:c_sapakhi.holder = { add_to_list = circassia_confederates }
		title:c_labezhi.holder = { add_to_list = circassia_confederates }
		title:c_boletuqo.holder = { add_to_list = circassia_confederates }
		title:c_miyequap.holder = { add_to_list = circassia_confederates }
		title:c_papagi.holder = { add_to_list = circassia_confederates }
		title:c_janeqo.holder = { add_to_list = circassia_confederates }
		title:c_siba.holder = { add_to_list = circassia_confederates }
		title:c_hatuqo.holder = { add_to_list = circassia_confederates }
		title:c_khegayk.holder = { add_to_list = circassia_confederates }
		title:c_zikhopol.holder = { add_to_list = circassia_confederates }
		title:c_aghuchips.holder = { add_to_list = circassia_confederates }
		title:c_tuahy.holder = { add_to_list = circassia_confederates }
		title:c_brijeqo.holder = { add_to_list = circassia_confederates }

		add_character_flag = {
			flag = forming_confederation
			years = 5
		}
		create_confederation = {
			name = CIRCASSIA_CONFEDERATION_NAME
		}

		every_in_list = {
			list = circassia_confederates

			scope:new_confederation = {
				add_confederation_member = prev
			}
		}
		remove_character_flag = forming_confederation
	}

	if = {
		limit = {
			game_start_date >= 1178.1.1
		}
		title:c_kyz-yar.holder = {
	
			add_character_modifier = {
				modifier = mpo_confederation_member_modifier
				years = 5
			}
			capital_county ?= {
				change_county_fertility = major_county_fertility_level_gain
			}
			title:c_khortytsia.holder = { add_to_list = white_cumania_confederates }
			title:d_kizilyedisan.holder = { add_to_list = white_cumania_confederates }
			title:d_crimea.holder = { add_to_list = white_cumania_confederates }
	
			add_character_flag = {
				flag = forming_confederation
				years = 5
			}
			create_confederation = {
				name = WHITE_CUMANIA_NAME
			}
			scope:new_confederation = {
				set_variable = {
					name = confederation_culture
					value = culture:cuman
				}
			}
	
			every_in_list = {
				list = white_cumania_confederates
	
				scope:new_confederation = {
					add_confederation_member = prev
				}
				add_character_modifier = {
					modifier = mpo_confederation_member_modifier
					years = 5
				}
				capital_county ?= {
					change_county_fertility = major_county_fertility_level_gain
				}
			}
			remove_character_flag = forming_confederation
		}
	
		title:d_don_valley.holder = {
	
			add_character_modifier = {
				modifier = mpo_confederation_member_modifier
				years = 5
			}
			capital_county ?= {
				change_county_fertility = major_county_fertility_level_gain
			}
			title:d_tana.holder = { add_to_list = black_cumania_confederates }
			title:d_khopyor.holder = { add_to_list = black_cumania_confederates }
			title:d_azov.holder = { add_to_list = black_cumania_confederates }
			title:d_sarkel.holder = { add_to_list = black_cumania_confederates }
			title:d_khazaria.holder = { add_to_list = black_cumania_confederates }
	
			add_character_flag = {
				flag = forming_confederation
				years = 5
			}
			create_confederation = {
				name = BLACK_CUMANIA_NAME
			}
			scope:new_confederation = {
				set_variable = {
					name = confederation_culture
					value = culture:cuman
				}
			}
	
			every_in_list = {
				list = black_cumania_confederates
	
				scope:new_confederation = {
					add_confederation_member = prev
				}
				add_character_modifier = {
					modifier = mpo_confederation_member_modifier
					years = 5
				}
				capital_county ?= {
					change_county_fertility = major_county_fertility_level_gain
				}
			}
			remove_character_flag = forming_confederation
		}
	}

#    title:k_caspian_steppe.holder = {
#        every_vassal = {
#            limit = { primary_title.tier >= tier_county }
#
#            add_to_list = cuman_confederates
#
#			create_title_and_vassal_change = {
#				type = independency
#				save_scope_as = change
#				add_claim_on_loss = no
#			}
#			becomes_independent = {
#				change = scope:change
#			}
#			resolve_title_and_vassal_change = scope:change
#        }
#
#	    destroy_title = title:k_caspian_steppe
#
#		add_character_flag = {
#			flag = forming_confederation
#			years = 5
#		}
#		create_confederation = {
#			name = CUMANIA_FEDERATION_NAME
#		}
#
#	    every_in_list = {
#	        list = cuman_confederates
#
#	        scope:new_confederation = {
#	            add_confederation_member = prev
#	        }
#	    }
#	    remove_character_flag = forming_confederation
#    }
}

ROA_setup_tributaries_effect = {
	if = {
		if = {
            limit = {
                game_start_date >= 867.1.1
                game_start_date <= 867.12.31
            }
        	ROA_867_tributary_setup_effect = yes
    	}
		else_if = {
            limit = {
                game_start_date >= 936.1.1
                game_start_date <= 936.12.31
            }
        	ROA_936_tributary_setup_effect = yes
    	}
    	else_if = {
            limit = {
                game_start_date >= 1066.1.1
                game_start_date <= 1066.12.31
            }
        	ROA_1066_tributary_setup_effect = yes
    	}
    	else_if = {
            limit = {
                game_start_date >= 1178.1.1
                game_start_date <= 1178.12.31
            }
        	ROA_1178_tributary_setup_effect = yes
    	}
    }
}

ROA_nominal_tributary_effect = {
	tributary_contract_set_obligation_level = { type = default_tributary_taxes level = 0 }
	tributary_contract_set_obligation_level = { type = default_tributary_levies level = 0 }
	tributary_contract_set_obligation_level = { type = default_tributary_prestige level = 1 }
}

ROA_weaker_tribute_effect = {
	tributary_contract_set_obligation_level = { type = default_tributary_taxes level = 1 }
	tributary_contract_set_obligation_level = { type = default_tributary_prestige level = 1 }
}

ROA_have_10_and_leave_me_be_effect = {
	tributary_contract_set_obligation_level = { type = default_tributary_taxes level = 1 }
	tributary_contract_set_obligation_level = { type = default_tributary_prestige level = 0 }
	tributary_contract_set_obligation_level = { type = default_tributary_levies level = 0 }
}

ROA_867_tributary_setup_effect = {

	#### Magyars
	title:k_magyar.holder = {
		save_scope_as = suzerain
		title:c_kherson.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Oghuz
	title:k_oghuz_il.holder = {
		save_scope_as = suzerain
		title:c_kos_uba.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mugodzhar_hills.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_tumar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_murun_tomis.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_chumkar_kia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_turgay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_jilali.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_murun_tomis.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_akkityk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kos-buwak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bekdas.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_garabogazkol.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_qyzylustyurt.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_jamantau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_airyuk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_terekli.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_sagiz.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_atyrau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_aral_karakum.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Pechenegs
	title:d_pecheneg.holder = {
		save_scope_as = suzerain
		title:c_yaitsk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Zhetysu
	title:k_zhetysu.holder = {
		save_scope_as = suzerain
		title:d_illiq.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bayanjurek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_saryesik.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Qirgiz
	title:e_kirghiz_khanate.holder = {
		save_scope_as = suzerain
		title:d_khamar-daban.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_otuken.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_ovorkhangai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_altaigobi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_altai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_ubsunur.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tannu_ola.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_qasar-qurug.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_ogrash.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_khentii.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_gorkhon.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_inzagata.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sagay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_biryus.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ubins.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kolyvan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kulunda.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_tomsk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_arik_usun.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Kimek
	title:k_kipchak.holder = {
		save_scope_as = suzerain
		title:d_lanikaz.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kurgan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_jitikul.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kush-murun.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_shchuchinsk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_sabakyul.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_karasu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kulan_utmas.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_atbasar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kaska.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_teniz.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ulytau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_astana.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_jangyztau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_petropavl.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_betpa.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_manaka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_emil.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_uliuty.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ku_jeku.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_uch_katyn.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Srivijaya
	title:e_srivijaya.holder = {
		save_scope_as = suzerain
		title:d_tanjungpura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_panai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_nagur.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_lamuri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_wijayapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sunda.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_langkasuka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_pahang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_beruas.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ujong_tanah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kedah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_terang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_takola.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_pariaman.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_siak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_batam.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bangka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_jambi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_darek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kerinci.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_pasaman.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_lampung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kampar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_indragiri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Kambuja
	title:e_kambujadesa.holder = {
		save_scope_as = suzerain
		title:c_pailin.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_can_tho.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mondul_kiri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_samraong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_battambang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_khemara_phoumin.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_koh_kong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Champa
	title:k_champa.holder = {
		save_scope_as = suzerain
		title:d_tribhuvanadevi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_vijaya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_panduranga.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Nanzhao
	# Lishui
	title:k_nanzhao.holder = {
		save_scope_as = suzerain
		title:d_kengtung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_meng_laa.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_phongsali.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_chiang_hung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mongkawng.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mongyang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_putao.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_luang_nam_tha.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_muang_thaeng.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_tu_long.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bao_lac.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ni_jiao.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Haripunjaya
	title:d_haripunjaya.holder = {
		save_scope_as = suzerain
		title:d_tak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ban_yaeng.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Lavapura
	title:d_lavapura.holder = {
		save_scope_as = suzerain
		title:c_u_thong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kabin_buri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_singhapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_si_thep.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Yongchang
	title:d_yongchang.holder = {
		save_scope_as = suzerain
		title:c_putao.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

#	# Beikthano
#	title:c_beikthano.holder = {
#		save_scope_as = suzerain
#		title:c_magwe.holder = {
#			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
#		}
#	}

	# Yinsheng
	title:d_kainan.holder = {
		save_scope_as = suzerain
		title:d_kengtung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_phongsali.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_chiang_hung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_meng_laa.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Shan
	title:d_yawnghwe.holder = {
		save_scope_as = suzerain
		title:c_yawnghwe.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:c_laikha.holder = {
		save_scope_as = suzerain
		title:c_mongkung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_north_shan.holder = {
		save_scope_as = suzerain
		title:c_manglon.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_kengtung.holder = {
		save_scope_as = suzerain
		title:c_chiang_hung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:c_chiang_hung.holder = {
		save_scope_as = suzerain
		title:c_meng_laa.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Mon
	title:d_thaton.holder = {
		save_scope_as = suzerain
		title:c_pegu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_hpapun.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:c_hpa_an.holder = {
		save_scope_as = suzerain
		title:c_kawkareik.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Annam
	title:k_jinghai.holder = {
		save_scope_as = suzerain
		title:d_nong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_thuy_vi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_lo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_tra_lan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_quynh_nhai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_yen_lo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Khazar Tributaries
	title:e_caspian-pontic_steppe.holder = {
		save_scope_as = suzerain
		title:c_pereyaslavl.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_romen.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_chernigov.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kursk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kozelsk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_novosil.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_voronezh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_mordvinia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_moksha.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_burtasia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_volga_bulgaria.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_boguchar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_yegorlyk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_arzgir.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_elista.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Caucasus
	title:k_abkhazia.holder = {
		save_scope_as = suzerain
		title:c_mdamuaa.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_zemo_svaneti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_misimianeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_kvemo_svaneti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}
	title:d_kakheti.holder = {
		save_scope_as = suzerain
		title:c_pkhoeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}
	title:d_arran.holder = {
		save_scope_as = suzerain
		title:c_elni.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_kapalak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect
		}
	}
	title:d_shirvan.holder = {
		save_scope_as = suzerain
		title:c_shahdagh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_kuba.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_derbent.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect
		}
	}
	title:e_byzantium.holder = {
		save_scope_as = suzerain
		title:d_zichia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_khegayk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}
	title:e_arabia.holder = {
		save_scope_as = suzerain
		title:d_turuberan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect = yes
		}
		title:k_armenian_principality.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect = yes
		}
	}
	title:k_armenian_principality.holder = {
		save_scope_as = suzerain
		title:d_taron.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect = yes
		}
	}	
	title:e_caspian-pontic_steppe.holder = {
		save_scope_as = suzerain
		title:c_abzakhia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_boletuqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_brijeqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_hatuqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_janeqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_labezhi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_miyequap.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_papagi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_sapakhi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_siba.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:d_chechnya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:d_khundzeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_dargia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_kara-kaitag.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_gazi-kumukh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:k_caucasus.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}

}

ROA_936_tributary_setup_effect = {

	#### Oghuz
	title:k_oghuz_il.holder = {
		save_scope_as = suzerain
		title:d_ouzoy.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_atyrau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_qoshagyl.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mangyshlak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mugodzhar_hills.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_manatau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_jilali.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_basagha.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_terekli.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_barsuki.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_achu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_aral_karakum.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kos-buwak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_turgay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_turgay.holder = {
		save_scope_as = suzerain
		title:c_murun_tomis.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_transcaspiana.holder = {
		save_scope_as = suzerain
		title:c_qyzylustyurt.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_garabogazkol.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Zhetysu
	title:k_zhetysu.holder = {
		save_scope_as = suzerain
		title:d_illiq.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bayanjurek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_saryesik.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Yenesei
	title:k_yenesei.holder = {
		save_scope_as = suzerain
		title:d_songualtai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tuva.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_shoria.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_biryus.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sagay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tofalar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Pechenegs
	title:k_pechenegs.holder = {
		save_scope_as = suzerain
		title:d_pereyaslavl.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_terebovl.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_bukovina.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Srivijaya
	title:e_srivijaya.holder = {
		save_scope_as = suzerain
		title:c_pariaman.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_darek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kampar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_siak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_indragiri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_jambi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_lampung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bangka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ujong_tanah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_beruas.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_pahang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kedah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_langkasuka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kerinci.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_terang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Champa
	title:k_champa.holder = {
		save_scope_as = suzerain
		title:d_tribhuvanadevi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_vijaya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_panduranga.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Kambuja
	title:e_kambujadesa.holder = {
		save_scope_as = suzerain
		title:d_can_tho.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mondul_kiri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_battambang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_canasapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_nong_kong_kaeo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_shrestapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sekong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Lavo
	title:d_lavapura.holder = {
		save_scope_as = suzerain
		title:c_u_thong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_singhapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_pribpri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kabin_buri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_si_thep.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Liao
	title:e_liao.holder = {
		save_scope_as = suzerain
		title:k_jin_china.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kherlen.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_shira-ke_er.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_wuluo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_hulan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_hoewon.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# China
	title:e_celestia_china.holder = {
		save_scope_as = suzerain
		title:d_liangzhou.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_dingnan_china.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Sri Gotapura
	title:e_kambujadesa.holder = {
		save_scope_as = suzerain
		title:d_nakhon_phanom.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Caucasus
	title:k_abkhazia.holder = {
		save_scope_as = suzerain
		title:c_mdamuaa.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_zemo_svaneti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_misimianeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_kvemo_svaneti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
	}
	title:d_kakheti.holder = {
		save_scope_as = suzerain
		title:c_pkhoeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
	}
	title:k_arran.holder = {
		save_scope_as = suzerain
		title:c_elni = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_kapalak = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect
		}
	}
	title:d_shirvan.holder = {
		save_scope_as = suzerain
		title:c_shahdagh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_kuba.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_derbent.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect
		}
	}
	title:k_ayrarat.holder = {
		save_scope_as = suzerain
		title:d_taron.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_weaker_tribute_effect = yes
		}
	}
	title:e_caspian-pontic_steppe.holder = {
		save_scope_as = suzerain
		title:c_abzakhia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_boletuqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_brijeqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_hatuqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_janeqo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_labezhi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_miyequap.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_papagi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_sapakhi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_siba.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:d_chechnya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:d_khundzeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_dargia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_kara-kaitag.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_gazi-kumukh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:k_caucasus.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		# Khazar Other
		title:c_voronezh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_moksha.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_burtasia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_elista.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_arzgir.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_manych.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

}

ROA_1066_tributary_setup_effect = {

	#### Cumania
	title:d_itil.holder = {
		save_scope_as = suzerain
		title:c_saray.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_pecheneg.holder = {
		save_scope_as = suzerain
		title:c_yaitsk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ukek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:k_pontic_steppe.holder = {
		save_scope_as = suzerain
		title:c_manych.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Qirgiz
	title:k_yenesei.holder = {
		save_scope_as = suzerain
		title:d_songualtai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_biryus.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tofalar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tuva.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_shoria.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sagay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Kimek
	title:d_alakol.holder = {
		save_scope_as = suzerain
		title:c_torangy_kul.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_agairik.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_narym_DZU.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Pechenegs
	title:k_pechenegs.holder = {
		save_scope_as = suzerain
		title:c_lower_dniepr.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Bashkirs
	title:d_bugurslan.holder = {
		save_scope_as = suzerain
		title:c_kbumak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_atamansku.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Pagan
	title:k_pagan.holder = {
		save_scope_as = suzerain
		title:d_arakan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_hsipaw.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_north_shan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_mongpai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kale.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bhamo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_nong_lu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_yebyu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_pashu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	title:c_kale.holder = {
		save_scope_as = suzerain
		title:c_thaungdut.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	title:c_takon.holder = {
		save_scope_as = suzerain
		title:c_mongyang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Kambuja
	title:e_kambujadesa.holder = {
		save_scope_as = suzerain
		title:d_lavapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_koh_kong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_mondul_kiri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_nakhon_phanom.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_nong_khai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_mahasay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sekong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Champa
	title:k_champa.holder = {
		save_scope_as = suzerain
		title:d_tribhuvanadevi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_vijaya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_panduranga.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Liao
	title:e_liao.holder = {
		save_scope_as = suzerain
		title:e_celestia_china.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_have_10_and_leave_me_be_effect
		}
		title:d_hoewon.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_arkhara.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_aigun_china.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_nonni.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kulusutai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kiyat.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_wuluo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_hulan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_shira-ke_er.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kherlen.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_khamar-daban.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_selenge.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_qasar-qurug.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tannu_ola.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_ubsunur.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_altai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_altaigobi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_otuken.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_tuula.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ogrash.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Chola hold over Srivijaya
	title:k_tamilakam.holder = {
		save_scope_as = suzerain
		title:d_kedah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:k_palembang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_beruas.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_panai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_lamuri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	title:k_palembang.holder = {
		save_scope_as = suzerain
		title:d_langkasuka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_pagaralam.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bangka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
#		title:c_beruas.holder = {
#			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
#		}
#		title:d_pahang.holder = {
#			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
#		}
#		title:c_ujong_tanah.holder = {
#			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
#		}
#		title:c_batam.holder = {
#			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
#		}
	}

    # Melayu
	title:d_jambi.holder = {
		save_scope_as = suzerain
		title:c_indragiri.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kerinci.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Dai Viet
	title:e_dai_viet.holder = {
		save_scope_as = suzerain
		title:d_muong.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_van_ban.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Dali
	title:k_nanzhao.holder = {
		save_scope_as = suzerain
		title:d_muang_thaeng.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_van_ban.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kengtung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_chiang_hung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_phongsali.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_vunsanh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_thuy_vi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Caucasus
	title:d_kakheti.holder = {
		save_scope_as = suzerain
		title:c_pkhoeti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
	}
	title:e_persia.holder = {
		save_scope_as = suzerain
		title:d_derbent.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_shahdagh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect
		}
		title:c_kuba.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kakheti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_transcaspiana.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_transcaspiana.holder = {
		save_scope_as = suzerain
		title:c_buzachi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Rus
	title:d_novgorod.holder = {
		save_scope_as = suzerain
		title:c_vodi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Wallachia and Moldavia
	title:k_pechenegs.holder = {
		save_scope_as = suzerain
		title:c_campulung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_polovragi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_giurgiu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bucuresti.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_buzau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_severin.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_iasi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_galati.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

}

ROA_1178_tributary_setup_effect = {

	#### Cumania
	title:c_khortytsia.holder = {
		save_scope_as = suzerain
		title:c_lozova.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_azov.holder = {
		save_scope_as = suzerain
		title:c_psije.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_grune.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_yegorlyk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_kizilyedisan.holder = {
		save_scope_as = suzerain
		title:c_tigheci.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_itil.holder = {
		save_scope_as = suzerain
		title:c_zhankala.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_pecheneg.holder = {
		save_scope_as = suzerain
		title:c_yaitsk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ukek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_khazaria.holder = {
		save_scope_as = suzerain
		title:c_elista.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_tana.holder = {
		save_scope_as = suzerain
		title:c_kuzeytorg.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Kipchak
	title:d_kokturgay.holder = {
		save_scope_as = suzerain
		title:c_achu.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Kimek
	title:d_jinghiz.holder = {
		save_scope_as = suzerain
		title:c_jinghiz.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_degelen.holder = {
		save_scope_as = suzerain
		title:c_ak-taylak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_tekar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Keraits
	title:k_otuken.holder = {
		save_scope_as = suzerain
		title:d_kiyat.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	#### Qirgiz
	title:k_yenesei.holder = {
		save_scope_as = suzerain
		title:d_songualtai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_biryus.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tofalar.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tuva.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_shoria.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_sagay.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Kara Qitai
	title:e_kara_khitai.holder = {
		save_scope_as = suzerain
		title:k_syr_darya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_ferghana.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_betpaqdala.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_karatau.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Jin
	title:e_jin.holder = {
		save_scope_as = suzerain
		title:d_hulan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_anwon.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_wuluo.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_yongwon.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_namhae.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Bashkirs
	title:d_bugurslan.holder = {
		save_scope_as = suzerain
		title:c_sakmara.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_atamansku.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Melayu/Dharmasraya
	title:k_melayu.holder = {
		save_scope_as = suzerain
		title:c_ujong_tanah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:k_palembang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_langkasuka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_tambralinga.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_kedah.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_panai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_pagaralam.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_darek.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_padang_lawas.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_siak.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_bangka.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_batam.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_pahang.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Kambuja
	title:e_kambujadesa.holder = {
		save_scope_as = suzerain
		title:d_lavapura.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_nong_khai.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_nakhon_phanom.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	### Champa
	title:k_champa.holder = {
		save_scope_as = suzerain
		title:d_tribhuvanadevi.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_vijaya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_panduranga.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

    # Nanzhao
	title:k_nanzhao.holder = {
		save_scope_as = suzerain
		title:d_chiang_hung.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:d_muang_thaeng.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_vunsanh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Rus
	title:d_novgorod.holder = {
		save_scope_as = suzerain
		title:c_izhora.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_kholmogory.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_totma.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_shenkursk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_opolye.holder = {
		save_scope_as = suzerain
		title:c_kunozh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_ustyug.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Caucasus
	title:d_chechnya.holder = {
		save_scope_as = suzerain
		title:c_tindia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
		title:c_didoya.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:d_shirvan.holder = {
		save_scope_as = suzerain
		title:c_shahdagh.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}
	title:d_zichia.holder = {
		save_scope_as = suzerain
		title:d_khegayk.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_sadzy.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
		title:c_zichia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}
	title:d_kassogia.holder = {
		save_scope_as = suzerain
		title:c_labeshkhua.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
	title:k_caucasus.holder = {
		save_scope_as = suzerain
		title:c_abazinia.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}

	# Persia
	title:k_persia.holder = {
		save_scope_as = suzerain
		title:k_adurbadagan.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			ROA_nominal_tributary_effect = yes
		}
	}
	title:k_adurbadagan.holder = {
		save_scope_as = suzerain
		title:k_daylam.holder = {
			break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
		}
	}
}
